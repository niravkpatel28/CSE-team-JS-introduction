// using id
let domSelection = document.getElementById("selectElements");
// using class
let topics = document.getElementsByClassName("topic");
// using attributes
let reference = document.querySelector("a[href]");
reference = document.querySelectorAll("a[target=_blank]");
console.log(reference);

// using parent child relations
